import { Box } from "@mui/material";
import React from "react";

function HomePage() {
  return (
    <Box
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "100vh",
      }}
    >
      Welcome to EXZI Home Page
    </Box>
  );
}

export default HomePage;
